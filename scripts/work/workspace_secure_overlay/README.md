# workspace_secure overlay

Здесь можно хранить дополнительные файлы, которые нужно положить поверх
основного `~/myconfig` после разворачивания bundle на VM.

Если нужны именно файловые override'ы, складывай их в:

- `scripts/work/workspace_secure_overlay/files/`
